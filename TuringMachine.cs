Functions =
   Movement:
        X(++|| --);
        Y(++|| --);
        Z(++|| --);
   Read:
        Character;
   Erase:
        Character;
   Write:
        Character;
   Optical:
        Zoom(++|| --);
````tape
% 0 Zoom out
# for Calculus and
// Linguistic
Semantics 
%1 Zoom in
# for `Pixel`

// -Storage
````.calabration
%2 ∀ must be 
# configured=
// 0 relative: 
X, Y and Z. 
```Slider
《 》
```.observer
∀
```.zoom
|C:《.!O ?,; N\∀(zy)xP》/E&
```X
|C:.!O ?,; N\《∀》(zy)xP/E&
```Y
[] { }<>-= "'`《∀》#lw@*h^i
```Z
RSTLAUHMDBC《∀》vfgjyk%~